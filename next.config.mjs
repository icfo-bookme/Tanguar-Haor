/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['127.0.0.1', 'localhost'], // Allow these domains for external images
    },
  };
  
  export default nextConfig;
  